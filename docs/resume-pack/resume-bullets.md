# Resume Bullets

Use these as starting points; tailor to the role and keep claims truthful to what you built.

- Built **ChronoFlow**, a distributed scheduler platform using **TypeScript/Node.js/Express** microservices, **Sequelize**, Kafka, Redis, and PostgreSQL, with an **Expo React Native** admin app, Kubernetes + Helm deployment, and GitHub Actions CI/CD to GHCR.
- Implemented API gateway auth (API keys), Redis-backed per-tenant rate limiting, Kafka-driven scheduling, webhook execution with retries/DLQ, and OpenTelemetry tracing with Prometheus/Grafana/Jaeger observability.
- Rewrote a Java/Spring Boot backend to a RockED-aligned JS stack while preserving PostgreSQL, Redis, Kafka, Docker Compose infra, Python e2e/chaos scripts, and deployment manifests.

## Elevator Pitch

ChronoFlow is a multi-tenant job scheduling platform that accepts cron jobs, dispatches executions through Kafka, performs resilient webhook delivery with persisted retries/DLQ, and provides end-to-end observability and production-ready Kubernetes deployment controls — now implemented in TypeScript/Express with an Expo mobile admin console.
